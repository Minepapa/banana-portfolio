# banana-portfolio — 기능 가이드 & 역할 정의

> 이 문서는 banana-portfolio 앱과 연동 시스템(Kakao-Notification·Trading Agent)의
> **기능별 역할·사용법·데이터 흐름**을 정리한 상설 참조 문서다. 새 기능 추가 시 갱신할 것.

---

## 1. 시스템 구성

| 구성 | 정체 | 역할 |
|------|------|------|
| **banana-portfolio** | React 웹앱(GitHub Pages) + Node 무인 스크립트(launchd) | 표시 + 무인 분석/적재 |
| **Kakao-Notification** | 안드로이드 앱(NotificationListener) | 입력 자동화 — 증권사 카카오 알림을 시트에 적재 |
| **Trading Agent** | 대화형 분석 정본(playbook·learning) | 평가·리포트의 "두뇌". 직접 보지 않음 |
| **공유 스프레드시트** | `1ANhZyJ…` | 모든 시스템이 공유하는 데이터 버스 |

---

## 2. 데이터 흐름

```
증권사 카카오 알림 → Kakao 앱 → "알람" 탭에만 원문 적재 (단순·신뢰)
                                        │
   banana parse-notifications.mjs (launchd 매시간, 멱등)
     → 알람 원문 파싱 → ├─ 체결(매수/매도) → "체결내역" 탭
                        ├─ 배당/분배금     → "배당금" 탭
                        ├─ 펀드 적립 매수   → 좌수 역산·누적 재계산 → 연금저축 펀드 행 덮어쓰기
                        ├─ 금현물 매수(g)   → 수량·투자금 누적 재계산 → 위탁 금 행 덮어쓰기
                        └─ 예수금          → NH 입금/출금 잔고 앵커 + 거래 델타 → 계좌별 예수금 행

스프레드시트 ─ banana 앱(11탭)이 읽어 표시
            ├ 평가요청(큐) ─ drain(3h) → 헤드리스 claude 평가 → 종목투자노트
            ├ 리스크모니터/기준선 ← risk-monitor(D 평일08:00 · B 월08:10)
            └ 주간리포트 ← sync-reports(매일09:00)가 weekly_report_*.md 적재

Trading Agent ─ portfolio-rebalancing-monitor 스킬(수동) → weekly_report_*.md 생성
```

**원칙**: Kakao=입력 자동화, Trading Agent=분석 두뇌, banana=표시+무인 실행.
체결/배당 파싱은 banana가 단일 담당(앱은 알람 원문만 — 누락 방지). 자세한 배경: `docs/plans` 및 핸드오프.

---

## 3. 앱 탭별 사용법 (언제·무엇)

| 탭 | 언제 | 목적 |
|----|------|------|
| 홈 / 자산분배 / 보유종목 | 수시 | 현황 조회·리밸런싱 갭 |
| 체결 / 배당금 / 수익금 | 수시 | 체결·배당·수익 내역 (배당 탭에서 **종목명 인라인 편집** 가능) |
| **평가** | 매수/매도 고민 시 | 종목을 평가요청 큐에 추가 → 5축 평가 카드 생성. "지금 살까/팔까?" |
| **노트** | 회고·근거 점검 | 적재된 평가 이력. 매도평가의 "최초 매수 이유" 비교 기준 |
| **리포트** | 주 1회 | 상단 "🎯 이번 주 행동 처방"(1가지) + 종합 |
| **KPI** | 월/회고 | TWR·Sharpe·MDD + 행동추적(500만 원칙·평가→매수 일치율) |
| **리스크** | 매일 1분 | 무인 조기경보 — 거시(D 일간)·논리(B 주간) 🔴🟡만 훑기 |

---

## 4. 자동화 잡

### launchd (무인, macOS) — `scripts/launchd/`
| 잡 | 주기 | 하는 일 |
|----|------|---------|
| `com.banana.parse-notifications` | 매시간 | 알람 원문 → 체결내역/배당금/펀드적립/금현물/예수금 멱등 파싱 (claude 미사용) |
| `com.banana.drain` | 3시간 | 평가요청 큐 → 헤드리스 claude 평가 → 종목투자노트 |
| `com.banana.risk-d` | 평일 08:00 | 거시 충격 신호 → 리스크모니터 |
| `com.banana.risk-b` | 월 08:10 | 위탁 개별주식 9종목 논리훼손 점검 → 리스크모니터 |
| `com.banana.report-sync` | 매일 09:00 | weekly_report_*.md → 주간리포트 시트 (누락 보충 안전망) |

설치: `bash scripts/launchd/install.sh`. 무인 실행은 서비스 계정 키(`~/.config/banana-portfolio/sa-key.json`) 필요.

### Google Apps Script (시트 내장 트리거)
| 파일 | 트리거 | 하는 일 |
|------|--------|---------|
| `scripts/benchmark-updater.gs` | 월간 | 월별잔고 I·J열(KOSPI·S&P500) 종가 |
| `scripts/gold-price.gs` | 1시간 | 국내 금 시세 등 현재가 갱신 |
| `scripts/legacy/benchmark-updater-rhino.gs` | (미사용) | benchmark-updater의 Rhino 호환 레거시판. GAS V8 런타임에선 불필요 |

---

## 5. 중복 점검의 역할 분담 (혼동 방지)

세 기능이 "보유 종목이 여전히 건전한가"를 다루지만 **목적·주기·산출물이 다르다.**

| 기능 | 주기 | 범위 | 산출물 | 역할 |
|------|------|------|--------|------|
| **risk-b** (자동) | 주 1회 | 위탁 개별주식 9 | 리스크 탭 신호(🟢🟡🔴) | **가드레일 조기경보** — 신호만, 행동 권고 없음 |
| **sell-evaluation** (온디맨드) | 트리거 시 | 특정 1종목 | 노트 탭 카드 | **매도 의사결정** — 최초 매수 이유 대비 비교 |
| **주간리포트 §진단** (수동) | 주 1회 | 전 보유 | 리포트 탭 | **종합 + 행동 처방** — risk 기준선/신호를 재사용 |

거시도 2중이나 역할이 다름: **risk-d**=일간 trigger 조기경보 / **주간리포트 §시장환경**=주간 종합 맥락.

> 권장: 주간리포트는 risk-monitor의 기준선·최근 신호를 **재인용**하고, 동일 데이터를 처음부터
> 재분석하지 말 것 (Trading Agent reporting 스킬 가이드 참조).

---

## 6. 자동 vs 수동 경계 (오해 방지)
- **체결내역/배당금**: 완전 자동 (Kakao 적재 → banana 파서). 누락 시 다음 시간에 자동 백필.
- **평가**: 큐에 넣으면 자동(drain) 또는 수동 평가. 큐가 비면 아무 일도 안 함.
- **주간 리포트**: **생성은 수동**(Trading Agent 스킬 실행) · **시트 적재는 자동**(sync-reports). 생성을 안 하면 리포트 탭이 안 갱신됨.
- **리스크 신호**: 완전 자동(launchd).

---

## 7. 핵심 규칙 (요약 — 정본은 CLAUDE.md / Trading Agent)
- 매수 원칙: 1회 **500만원 미만** 분할.
- 평가 결론 어휘: **🟢 유효 / 🟡 관망 / 🔴 부적합 / ⚪ 판단보류** (통일).
- 평가 데이터: 요청일 기준 **직전 분기** 우선.
- 행동추적 평가→매수 매칭: **30일** 윈도, 미경과분은 "유예"(집계 제외).
