# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local Netlify folder
.netlify

# Claude Code / OMC session metadata
# .claude/* 로 직계 자식만 무시 — agents/(부서 에이전트 정의, 경로 참조만 포함·전략 내용 없음)는
# 코드(agent-loader)가 의존하므로 네거티브 패턴으로 버전관리에 포함한다.
# 단 agents/memory/(부서 메모리·업무기록)는 실제 투자 활동 내용이라 로컬 전용(profile/ 정책과 일치).
.claude/*
!.claude/agents/
!.claude/commands/
!.claude/settings.json
.claude/agents/memory/
.omc/
CLAUDE.md

.env

scripts/.cache/
__pycache__/
*.pyc

# 발행된 주간 리포트(실제 잔고 포함)·투자 성향 프로필(재무 정보) — 비공개
# ⚠️ 트레일링 슬래시 없음(2026-08-17 수정) — v1 리포(banana-portfolio)를 가리키는
# 심볼릭 링크로 존재한다(v1/v2가 같은 정책·playbook·학습모듈을 공유). "profile/"처럼
# 끝에 슬래시가 붙은 패턴은 디렉토리만 매칭하고 심볼릭 링크는 매칭 대상에서 빠져서,
# 의도한 "로컬 전용"이 실제로는 전혀 작동하지 않고 있었다(git status에 ??로 계속
# 뜨고 있었음, 코드리뷰 중 발견) — 슬래시를 빼 파일·디렉토리·심볼릭 링크 전부 잡히게 함.
reports
profile
# 평가 playbook·학습 모듈·에이전트 스킬(개인 투자 전략·KPI·자기개선 루프) — Trading Agent에서 이전, 로컬 전용
playbooks
learning
skills
